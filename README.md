# SecureFileTransferSystem
It is Java-based secure file transfer system using TCP sockets, AES encryption, and SHA-256 hashing. Supports multi-client access with authentication and ensures reliable, secure data transmission.
